"use client";

/**
 * Guestbook widget — placeholder stub.
 * The real implementation (form + entry list backed by /api/guestbook) replaces this.
 */
export default function GuestbookPanel() {
    return (
        <div
            aria-busy="true"
            className="rounded-channel border border-wii-line bg-wii-white p-6 text-wii-text shadow-wii-sm"
        >
            Guestbook is warming up…
        </div>
    );
}
