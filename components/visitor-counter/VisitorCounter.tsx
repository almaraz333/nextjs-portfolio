"use client";

/**
 * Visitor counter widget — placeholder stub.
 * The real implementation (odometer backed by /api/visits) replaces this.
 */
export default function VisitorCounter() {
    return null;
}
